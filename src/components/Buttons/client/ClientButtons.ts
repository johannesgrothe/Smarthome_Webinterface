// TODO:    buttons should be reusable, meaning they should be generic, so that the may be reused,
//          which means that everything the button needs, must be passed to it (hooks, etc.)
//          check whether this can be done in a reasonable scope

export function RebootClient() {
    // TODO: send reboot request to api, for specified client
}


